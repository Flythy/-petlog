# 宠记 PetLog 首页重构改动说明

> 原型文件：[petlog-app-prototype-moe.html](file:///workspace/petlog-app-prototype-moe.html)
> 整理日期：2026-09-20

## 一、需求背景

本次围绕首页共提出三项改进：

1. **宠物基础信息太少**：当前只展示名字、头像，需要增加 **年龄、体重、品种** 信息。
2. **今日待办信息不清晰**：改为类似「上次驱虫：8月2日，建议每30天一次」的格式，让用户一眼看懂「上次什么时候做的、建议多久做一次」。
3. **首页样式割裂**：参考「宠拍档」APP/小程序首页，保留现有可爱色调与全部模块，解决当前首页被分割成多个条条块块、不够好看的问题。

## 二、改动总览

| 改动项 | 改动前 | 改动后 |
| --- | --- | --- |
| 宠物基础信息 | 仅头像 + 名字 | 头像 + 名字 + 「品种 · 年龄 · 性别」+ 体重胶囊 |
| 今日待办描述 | 「小白 · 已超 7 天，赶紧处理」 | 「上次体内驱虫：6月15日，建议每90天一次 · 海乐妙 · 1 片」 |
| 首页布局 | 多个独立白色卡片堆叠、各带阴影 | 顶部渐变区 + **一张大白卡**包裹全部功能，模块间用柔和分隔线区隔 |

核心原则：**所有 id 与数据绑定逻辑全部保留**，不破坏原有 JS 渲染，只调整 HTML 结构、补充数据字段并追加 CSS 覆盖样式。

## 三、首页新结构

首页重构为「顶部渐变区 + 一张大白卡」两层结构（[petlog-app-prototype-moe.html](file:///workspace/petlog-app-prototype-moe.html) 首页 `home-shell` 区块）：

```html
<div class="home-shell">
  <!-- ① 顶部渐变区：宠物切换 + 宠物基础信息（白字融入渐变） -->
  <div class="hs-top">
    <div class="pet-seg" id="home-pet-switch">…</div>
    <div class="pbc-top" data-open="pg-pet-detail">
      <img class="pbc-avatar" id="pbc-avatar" src="" alt="">
      <div class="pbc-info">
        <b id="pbc-name"></b>
        <span class="pbc-meta" id="pbc-meta"></span>
        <span class="pbc-weight">
          <svg class="ico xs"><use href="#i-scale"/></svg>
          <span id="pbc-weight"></span>
        </span>
      </div>
      <svg class="ico pbc-go"><use href="#i-chev-r"/></svg>
    </div>
  </div>

  <!-- ② 大白卡：统一包裹全部功能模块 -->
  <div class="hs-card">
    <div class="pbc-ql-wrap">…快速记录（id="hl-row"）…</div>
    <div class="hs-div"></div>

    <div class="section-head">…今日待办…</div>
    <div id="home-todo-list"></div>
    <div class="hs-div"></div>

    <div class="food-quick" id="home-food-scan">…能吃什么入口…</div>
    <div class="hs-div"></div>

    <div class="ab-mini" id="ai-brief-mini">…AI 健康简报…</div>
  </div>
</div>
```

设计要点：

- **顶部品牌色橙粉渐变保留**，并向屏幕顶部自然延伸；宠物切换与基础信息以白字融入渐变。
- **快速记录、今日待办、能吃什么、AI 健康简报**四大模块全部整合进同一张白色大圆角卡片，彻底消除独立卡片阴影造成的割裂感。
- 模块之间用 `.hs-div` 柔和水平分隔线区隔，而非独立卡片。
- 保留两个 AI 入口（能吃什么、AI 健康简报），呼应宠拍档「多专业智能体」的设计思路。

## 四、宠物基础信息增强

### 展示内容

- 第二行 `.pbc-meta`：`品种 · 年龄 · 性别`，例如「英国短毛猫 · 约 2岁 6个月 · 男生」。
- 第三行 `.pbc-weight`：体重胶囊，带体重秤图标，例如「4.2kg」。

### 渲染逻辑

在 [applyHomePet](file:///workspace/petlog-app-prototype-moe.html#L7030-L7030) 中新增对 `pbc-meta` 的同步渲染，复用现有工具函数，保证与宠物档案页口径一致：

- [petAge](file:///workspace/petlog-app-prototype-moe.html#L3497-L3497)：生成年龄文案（如「约 2岁 6个月」）。
- [petGenderLabel](file:///workspace/petlog-app-prototype-moe.html#L3517-L3517)：将性别转为「女生 / 男生」。

```javascript
var pbcMeta = document.getElementById('pbc-meta');
if (pbcMeta) {
  var ageInfo = petAge(p.birthISO, p.birthApprox);
  var metaParts = [
    p.breed,
    ageInfo ? ageInfo.text : null,
    p.gender ? petGenderLabel(p.gender) : null
  ];
  pbcMeta.textContent = metaParts.filter(Boolean).join(' · ');
}
```

## 五、今日待办信息增强

### 1. 待办数据新增字段

在 [HOME_PETS](file:///workspace/petlog-app-prototype-moe.html#L4318-L4318) 的示例待办中，为每条待办补充两个字段：

| 字段 | 含义 | 示例 |
| --- | --- | --- |
| `lastISO` | 上次完成日期（ISO） | `'2026-06-15'` |
| `intervalDays` | 建议间隔天数 | `90` |

示例数据：

- 小白（约 [petlog-app-prototype-moe.html#L4339-L4340](file:///workspace/petlog-app-prototype-moe.html#L4339-L4340)）：体内驱虫 `lastISO:'2026-06-15', intervalDays:90`；狂犬疫苗 `intervalDays:365`。
- 豆豆（约 [petlog-app-prototype-moe.html#L4362-L4363](file:///workspace/petlog-app-prototype-moe.html#L4362-L4363)）：体外驱虫 `intervalDays:30`；年度体检 `intervalDays:365`。

### 2. 数据透传修复

[getTodayTodos](file:///workspace/petlog-app-prototype-moe.html#L6492-L6492) 在映射待办时补充透传 `lastISO` / `intervalDays`，否则描述函数读不到新字段：

```javascript
return {
  src:'care', uid:t.uid, ico:t.ico, icon:t.icon, title:t.title, desc:t.desc,
  type:t.type||t.title, meta:t.meta, next:t.next, tint:t.tint,
  lastISO:t.lastISO, intervalDays:t.intervalDays, raw:t
};
```

### 3. 新描述格式

[todoProblem](file:///workspace/petlog-app-prototype-moe.html#L6527-L6527) 改为优先生成「上次事项 + 日期 + 建议间隔」文案，再拼接药品 / 备注：

```javascript
function todoProblem(it){
  var d = (it.desc || '').replace(/^[^·]+·\s*/, '');
  if (it.lastISO && it.intervalDays) {
    var dt = parseISO(it.lastISO);
    var dateLabel = dt ? (dt.getMonth()+1)+'月'+dt.getDate()+'日' : it.lastISO;
    var thing = it.type || it.title || '';
    var head = '上次'+thing+'：'+dateLabel+'，建议每'+it.intervalDays+'天一次';
    return head + (it.meta ? ' · '+it.meta : '');
  }
  if (!d && it.meta) d = it.meta;
  return d || it.meta || '';
}
```

实际效果：

- 小白：「上次体内驱虫：6月15日，建议每90天一次 · 海乐妙 · 1 片」
- 豆豆：「上次体外驱虫：8月18日，建议每30天一次 · 拜宠清 · 1 支」

## 六、一体化 CSS 样式

新增的覆盖样式集中在文件约 [petlog-app-prototype-moe.html#L1663-L1663](file:///workspace/petlog-app-prototype-moe.html#L1663-L1663) 起的「一体化首页 shell」注释区块，所有规则均限定在首页作用域，不影响其他页面。主要包含：

- `.hs-top`：顶部渐变区内边距；`.pbc-top` 使用 flex 排列头像与信息。
- `.hs-top .pbc-avatar`：64px 圆角头像，白色描边 + 柔和阴影，轻微旋转更显可爱。
- `.hs-top .pbc-info b`：22px 白色圆体名字；`.pbc-meta` 为半透明白色小字；`.pbc-weight` 为半透明白底胶囊。
- `.hs-card`：白色 30px 大圆角卡片，统一暖色投影。
- `.hs-div`：高 1px 的 `#F6ECE2` 柔和分隔线。
- 卡内各模块重置：
  - 快速记录 `.pbc-ql-wrap` / `#hl-row`：去掉自带白底、圆角与阴影，透明融入。
  - 待办 `.ht-card`：改为 `#FDF8F3` 浅色「行」，无外阴影。
  - 能吃什么 `.food-quick`、AI 简报 `.ab-mini`：收窄为 20px 圆角内嵌行，去掉独立阴影。

## 七、验证结果

- 已在浏览器分别验证 **小白（猫）** 与 **豆豆（狗）**：
  - 宠物切换正常，品种 / 年龄 / 性别 / 体重均正确渲染。
  - 待办描述正确显示「上次事项 + 日期 + 建议间隔 + 备注」新格式。
  - 快速记录、今日待办、能吃什么、AI 健康简报均位于同一张大白卡内，模块间以分隔线区隔，无割裂感。
- 结构检测确认 AI 简报位于大白卡内部（`inside: true`）。
- 浏览器控制台无业务报错。

## 八、涉及代码位置索引

| 内容 | 位置 |
| --- | --- |
| 一体化首页 CSS | [petlog-app-prototype-moe.html#L1663](file:///workspace/petlog-app-prototype-moe.html#L1663-L1663) |
| 首页宠物数据 HOME_PETS | [petlog-app-prototype-moe.html#L4318](file:///workspace/petlog-app-prototype-moe.html#L4318-L4318) |
| 小白待办示例 | [petlog-app-prototype-moe.html#L4339-L4340](file:///workspace/petlog-app-prototype-moe.html#L4339-L4340) |
| 豆豆待办示例 | [petlog-app-prototype-moe.html#L4362-L4363](file:///workspace/petlog-app-prototype-moe.html#L4362-L4363) |
| petAge 年龄计算 | [petlog-app-prototype-moe.html#L3497](file:///workspace/petlog-app-prototype-moe.html#L3497-L3497) |
| petGenderLabel 性别文案 | [petlog-app-prototype-moe.html#L3517](file:///workspace/petlog-app-prototype-moe.html#L3517-L3517) |
| getTodayTodos 待办透传 | [petlog-app-prototype-moe.html#L6492](file:///workspace/petlog-app-prototype-moe.html#L6492-L6492) |
| todoProblem 新描述格式 | [petlog-app-prototype-moe.html#L6527](file:///workspace/petlog-app-prototype-moe.html#L6527-L6527) |
| applyHomePet 首页渲染 | [petlog-app-prototype-moe.html#L7030](file:///workspace/petlog-app-prototype-moe.html#L7030-L7030) |
