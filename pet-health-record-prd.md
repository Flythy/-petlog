# 宠物健康档案 + 提醒 · 功能设计文档（PRD v1）

> 产品定位：AI 养宠决策助手的「留存钩子 + 数据入口」，不是独立目的地。
> 一句话价值：让主人「记得住、记得清、少操心」，同时把散落的健康数据结构化，回流给 AI 问答/推荐，形成数据飞轮。

---

## 1. 设计原则（所有功能的取舍标准）

1. **极低录入成本**：任何一条记录 3 秒内能录完；能默认就不问，能选就不填，能拍就不打。
2. **提醒是价值锚点，不是骚扰**：精准、低频、可关，且每条提醒必须带「下一步行动」。
3. **数据即资产**：记录全部结构化，可回填 AI 问答与个性化推荐。
4. **信任闭环**：每个提醒/异常都是自然引导到「问诊 / 严选 / 保险」的触点，但绝不硬广。

---

## 2. 目标用户与核心场景

- **新手铲屎官（最大盘子）**：不知道该打什么、什么时候打，怕漏、怕错。
- **多宠家庭**：多只宠物记混、搞不清谁该驱虫。
- **老年/慢病宠物家长**：用药、复查、体重监测频次高，最需要兜底。

核心场景（按频次）：
1. 「我家猫是不是该驱虫/打疫苗了？」→ 提醒替用户记住。
2. 「上次打了什么疫苗/吃了什么药？记不清了」→ 结构化档案可回溯。
3. 「体重超标了没？体检异常项怎么办？」→ 曲线 + 异常标记 + 引导问诊。

---

## 3. 信息架构

```
宠物管理（多宠物）
└─ 单宠物主页
   ├─ 1. 宠物档案       基础信息 / 身份凭证 / 健康基线
   ├─ 2. 健康记录       疫苗 / 驱虫 / 体检 / 体重 / 病史 / 用药 / 手术
   ├─ 3. 提醒引擎       周期 / 单次 / 自定义 / 智能推断（★核心）
   ├─ 4. 成长分析       体重曲线 / 里程碑 / 健康评分
   └─ 5. 信任闭环出口    症状→AI初判→就医·购药 / 保险兜底
```

---

## 4. 功能详细设计

### 4.1 宠物档案（建立信任的第一屏）

| 分类 | 字段 | 说明 |
|---|---|---|
| 基础 | 昵称、品种、生日/领养日、性别、是否绝育、毛色 | 品种联动「品种库」 |
| 身份 | 芯片号、宠物证、疫苗本照片 | 拍照留存，OCR 可后置 |
| 健康基线 | 出生体重、当前体重、过敏史、慢性病、遗传病风险 | 供 AI 推荐使用 |
| 来源 | 购买 / 领养 / 捡到 | 影响内容与推荐策略 |

关键设计：**品种库**内置主流犬猫品种的疫苗/驱虫/体检标准模板，作为默认提醒依据；自动换算「人龄」，用于体检分级（老年宠物更频繁）。

### 4.2 健康记录（多类型，统一时间线）

每种记录都含 `记录时间` + `下次时间` 两个字段，`下次时间` 自动进入提醒引擎。

| 类型 | 关键字段 | 下次时间默认规则 |
|---|---|---|
| 疫苗 | 种类、第几针、品牌/批次 | 按针次+间隔（如猫三联 8/12/16 周龄，狂犬 1 年） |
| 驱虫 | 内驱/外驱/同驱、药物、剂量 | 内驱 3 个月 / 外驱 1 个月（可调） |
| 体检 | 项目、结果、报告照片、异常项、医生建议 | 成年 1 年 / 老年(7+) 6 个月 |
| 体重 | 数值 | 无（自动生成曲线） |
| 病史 | 症状、诊断、治疗、用药、花费、医院/医生、是否治愈 | 复查时间（医生建议） |
| 用药 | 药品、剂量、频率、疗程起止 | 每日/每 N 小时，至疗程结束 |
| 手术/绝育/牙科 | 类型、日期、医院、术后护理 | 术后复诊时间 |

**录入降门槛（关键）**：
- 快速录入：默认值 + 选择器 + 语音输入，3 秒完成。
- **拍照 OCR/AI 识别（V1）**：疫苗本、化验单、处方单、驱虫记录 → 拍照/选图 → AI 自动提取结构化字段（种类、日期、药物、剂量等）→ 用户确认或修正 → 一键保存。支持批量识别（一张照片含多条记录）。识别结果自动填充到对应记录类型，`下次时间` 按品种库规则自动计算。
- 医院/体检机构数据导入（V2，B 端打通后）。

**OCR 识别流程**：
1. 用户在「新建记录」页点击「拍照识别」或在首页快捷入口进入。
2. 拍照或从相册选择疫苗本/处方单/化验单照片。
3. AI 识别中（加载动画，预计 2-5 秒）。
4. 展示识别结果卡片：每条提取的记录以可编辑卡片呈现，用户可修改/删除/新增。
5. 用户确认 → 批量保存到健康记录 → 自动触发提醒引擎。

### 4.3 提醒引擎（★ 核心价值锚点）

**触发类型**：
- 周期提醒：疫苗、驱虫、体检。
- 单次提醒：复查、用药、术后复诊。
- 自定义提醒：洗澡、剪指甲、买粮、保险续费。
- 智能推断：基于记录 + 品种标准，AI 判断「该打 XX / 该驱虫 / 该体检了」。

**推送策略（防骚扰）**：
- 渠道：微信订阅消息（一次性模板，用户主动触发授权）。
- 分级触达：T-7 预告 → T-1 提醒 → T 当天 → 逾期 3 天催办（最多 2 次催办）。
- 频率上限：默认每宠物每周推送 ≤ 3 条，可配置。
- 免打扰时段：22:00–8:00 不推。
- 同类合并：多宠物同类型提醒合并为一条汇总。

**文案必须带行动卡片**（决定转化）：
> 【猫】小白 距上次驱虫已 43 天，建议 3 天内做内外驱 → [查看驱虫方案 / 去严选]

### 4.4 成长分析（留存抓手）

- 体重曲线：对比品种标准区间，超标/消瘦预警。
- 里程碑时间线：出生 → 首次疫苗 → 绝育 → … 的可视化。
- 健康评分：由「记录完整度 + 体重 + 疫苗驱虫合规度」合成，作为信任/留存抓手。

### 4.4b AI 个性化养护建议（V1）

基于宠物的完整健康档案（品种、年龄、体重趋势、疫苗/驱虫记录、病史、用药史），AI 生成针对性的养护建议，替代通用的百科知识。

**触发方式**：
- 宠物档案页「养护建议」入口。
- 健康分解读页底部「查看养护建议」。
- 首页智能卡片（当检测到可优化项时主动展示）。

**建议内容结构**：
- **饮食建议**：基于品种、年龄、体重趋势、是否绝育，给出喂食量/频次/营养侧重建议。
- **运动建议**：基于品种特性、年龄、体重状态，给出运动量/频次/注意事项。
- **疫苗/驱虫提醒优化**：基于当前合规度和品种标准，建议是否需要补打/提前。
- **体检建议**：基于年龄阶段（幼年/成年/老年）和病史，建议体检项目和频次。
- **季节性提醒**：根据当前季节和地区，提示当季高发疾病预防（如春季蜱虫、冬季呼吸道）。

**合规边界**：
- 每条建议标注「基于档案数据生成，仅供参考」。
- 涉及用药/治疗的建议一律引导「请咨询兽医」。
- 不做诊断，只做预防性养护指导。

### 4.5 信任闭环出口（变现衔接，V2 后置）

- 异常症状 → 一键触发 AI 初判 → 「观察 / 购药 / 就医」分级建议。
- 就医记录沉淀病历，下次就医可复用。
- 到期提醒引导预约合作医院 / 购买保险。

---

## 5. 核心数据模型（关键表）

- `pet`：id, user_id, name, breed_id, birthday, sex, neutered, allergy, chronic, source
- `pet_vaccine`：id, pet_id, type, dose_no, brand, batch, shot_at, next_at
- `pet_deworm`：id, pet_id, kind(内/外/同驱), drug, dose, done_at, next_at
- `pet_weight`：id, pet_id, weight, recorded_at
- `pet_medical`：id, pet_id, symptom, diagnosis, treatment, cost, hospital, cured
- `pet_medication`：id, pet_id, drug, dose, frequency, start_at, end_at
- `reminder`：id, pet_id, type, rule, next_trigger_at, status
- `reminder_log`：id, reminder_id, triggered_at, channel, clicked
- `health_score`：pet_id, score, updated_at

---

## 6. 关键用户流程

1. **建宠（≤30 秒）**：选品种 → 生日 → 性别 → 体重 → 完成（品种库自动预置默认提醒）。
2. **录一条疫苗/驱虫（3 秒）**：点「+」→ 选类型 → 默认下次时间自动算好 → 确认。
3. **提醒闭环**：订阅消息 → 点击行动卡片 → 完成动作 → 回填记录 → 下次时间自动顺延。

---

## 7. MVP 范围与迭代

- **V1（验证留存）**：多宠物档案 + 品种库；疫苗/驱虫/体重记录；**拍照 OCR 识别录入**；周期提醒（订阅消息）；体重曲线；简单健康分；**AI 个性化养护建议**。
- **V1.5**：体检/病史/用药记录；拍照 OCR；自定义提醒；AI 智能推断。
- **V2**：就医问诊衔接；医院/保险对接；报告解析；B 端数据打通。

---

## 8. 验证指标

| 指标 | 目标含义 |
|---|---|
| 建宠完成率 | 录入门槛是否够低 |
| D7 / D30 留存 | 提醒+档案是否形成习惯 |
| 提醒触达率 / 点击率 / 行动完成率（回填率） | 提醒引擎是否有效 |
| 单宠物记录数 / 记录完整度 | 数据资产厚度 |
| 提醒→导购 / 问诊转化率 | 信任闭环能否变现 |

---

## 9. 风险与合规

- **提醒骚扰**：频率上限 + 免打扰 + 一键关闭 + 文案带行动（不是纯通知）。
- **医疗合规**：定位「管理提醒」，非「诊疗建议」；异常一律引导就医；页内免责声明。
- **隐私**：宠物健康数据按敏感数据处理，明示用途与删除入口。
- **录入门槛**：OCR + 品种默认模板，把「该填什么」的认知负担降到零。
